# Practical Exam 2B2 — C# / Windows Forms

Complete practical exam project with two forms: average speed and pyramid volume calculations (`Form1`), and a nested loop exercise (`Form2`).

**Student ID:** `12100161` (the same student ID used in the Mesadinha project and the Computing Environments activities).

## Actual File Dates

The dates below are based on the timestamps of each file inside the original `.zip`, automatically generated by Visual Studio when the files were saved or compiled:

- `Program.cs` — 08/17/2022, 13:17
- `Form1.cs` / `.resx` / `.Designer.cs` — 08/17/2022, 13:19
- `Form2.cs` / `.resx` / `.Designer.cs` — 08/17/2022, 13:20
- `.csproj` — 08/17/2022, 13:20

## What Each Form Does

- **Form1** — Calculates average speed (distance traveled ÷ duration) and the volume of a pyramid based on its edge length and height.

- **Form2** — Uses a `while` loop from 0 to 15, with a special nested loop between the values 5 and 9, printing the numbers traversed.

## `compiled-evidence/` Folder

Contains the compiled executable itself (`Prova_2B2_12100161.exe`) and the hidden `.vs` folder (with the Visual Studio `.suo` file), providing evidence that the project was actually opened, edited, and compiled in Visual Studio rather than being written as standalone source code.